name: Test Action
on: [push]

jobs:
  job-1:
    runs-on: ubuntu-latest
    env:
      FOO: bar
    steps:
      - run: echo One Two Three
      - uses: ./actions/with-pre
      - uses: ./actions/with-pre
