name: Test Action
on: [push]

jobs:
  job-1:
    runs-on: ubuntu-latest
    name: "Test type coercion"
    strategy:
      matrix:
        - a: [1,2,3]
    steps:
      - run: echo ${{ matrix.a.2 }} ${{ matrix.a.1 }}
      - run: echo ${{ matrix.a[2] }} ${{ matrix.a[1] }}
