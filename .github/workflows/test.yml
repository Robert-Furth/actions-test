name: Test Action
on: [push]

jobs:
  job-1:
    runs-on: ubuntu-latest
    env:
      FOO: bar
    steps:
      - run: echo FOO=BAZ >> $GITHUB_ENV
      - run: echo ${{ env.FOO }} $FOO
      - env:
          FOO: qux
        run: echo ${{ env.FOO }} $FOO
      - run: echo ${{ env.FOO }} $FOO
