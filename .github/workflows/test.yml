name: Test Action
on: [push]

jobs:
  job-1:
    runs-on: ubuntu-latest
    env:
      FOO: bar
    steps:
      - uses: actions/checkout@v3
      - run: echo One Two Three
      - uses: ./actions/with-pre
      - uses: ./actions/with-pre
