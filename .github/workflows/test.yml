name: Test Action
on: [push]

jobs:
  job-1:
    runs-on: ubuntu-latest
    name: "Test type coercion"
    strategy:
      matrix:
        foo: [[]]
    steps:
      - run: echo ${{ matrix.foo || matrix.foo }}
